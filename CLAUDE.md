# 기출 직행 — 작업 규칙

## 연도는 항상 "년"인지 "학년도"인지 밝힌다

사용자에게 연도를 말할 때 **시행 연도인지 학년도인지 반드시 명시한다.**
"2015년"처럼 맨숫자로 두지 않는다.

- **평가원 시험**(수능·6월·9월 모의평가) — 시행 연도 + 1 = 학년도
  - `2015년 시행(2016학년도) 수능`
  - `2026년 6월 시행(2027학년도) 모평`
- **교육청 전국연합학력평가** — 관례상 시행 연도로만 부른다. 학년도를 붙이지 않는다
  - `2026년 7월 학평(인천)`

저장소 데이터(`index.html`의 payload)와 화면 표시는 **전부 시행 연도 기준**이다.
연도 드롭다운만 `2026년(2027학년도)`처럼 병기한다.

## 이 저장소에서 이미 확인된 사실 (다시 조사하지 말 것)

**파일 이름은 과목을 나타내지 않는다.** 2015년 시행(2016학년도) 수능이 대표적으로,
한국사 문제지가 `s_hanji`(한국지리), 한국지리가 `s_saeji`(세계지리) 이름을 달고 있다.
PDF를 열어 확인한 결과 내용은 EBSi가 붙인 행과 일치했다 — 이름만 어긋난 것이다.
**이름이 과목과 다르다는 이유로 자료를 고치거나 버리면 안 된다.**

의심스러운 회차는 PDF를 받아 첫 페이지를 이미지로 렌더링해 표지의 과목명을 읽어
확인한다(`pymupdf`). 파일명 비교로는 판단하지 않는다.

**출제 기관은 제목 글자로 가른다.** `index.html`의 `kindOf()`가 제목이 "수능"으로
시작하거나 "평가원"을 포함하는지로 평가원/교육청을 나눈다. 제목을 바꾸면 분류가
뒤집힐 수 있으므로, 기존 회차의 제목은 함부로 다시 쓰지 않는다.

**자료 갱신은 덧붙이기만 한다.** `tools/refresh_data.py`는 기존 기록을 고치거나
지우지 않고, 아직 없는 시행일만 채운다. 삭제가 필요하면 일회성으로 처리하고
스크립트에는 넣지 않는다.

**페이지가 스스로 앱으로 넘기지 않는다.** 안드로이드로 보인다고 `intent:`로
이동시키면 검색엔진 렌더러와 성능 측정 도구까지 함께 걸려 문서가 통째로 사라진다.
푸터의 '앱으로 열기'처럼 **누를 때만** 넘어간다. UA로 크롤러를 골라내는 방식으로
되돌리지 않는다(`tests/test_toapp.py`가 지킨다).

**APK를 저장소에 커밋하지 않는다.** 판본마다 1.8MB가 히스토리에 영구히 쌓이고
받는 사람 전부가 그 비용을 낸다. 한때 33판 57.7MB가 쌓여 있었고, 히스토리를
다시 써서 걷어냈다(`.git` 89MB → 12MB). `app/`에 들어가는 것은 `latest.json`
하나뿐이고, APK는 **GitHub 릴리스 자산**으로만 나간다. 다시 커밋하면 그 작업이
통째로 되돌아간다.

**서명 키는 어떤 경우에도 커밋하지 않는다.** `android/gijul-release.jks`와
`keystore.properties`는 gitignore 대상이며 저장소 뿌리의 `.gitignore`가 어느
폴더에 있든 잡는다. 인증서 지문은 `54:37:0E:D9:…:2C:FD:10:C1` 하나로 고정이다 —
이게 바뀐 APK는 기존 기기가 **영구히** 거부하고(지우고 새로 깔아야 하며 받아둔
자료도 사라진다), `docs/assetlinks.json`의 앱 링크도 함께 깨진다. `android.yml`의
'서명 확인' 단계가 매 빌드 대조하니 그 단계를 지우지 않는다.

**수능 시행일은 셈한다 — 11월 13~19일 사이의 목요일.** 7일 창이라 목요일이 딱
하나뿐이어서 해만 알면 날이 정해진다. 2016년 시행분부터 이 기준이 정착했고 그
뒤로 예외가 없다(2015년까지는 '11월 둘째 주'가 기준이라 한 주 빨랐다). 요일이
목요일인 것은 2007년 시행분부터이며, 문제지 수송 차량이 주말 고속도로 혼잡을
피하도록 수요일에서 옮긴 것이다. **"11월 셋째 목요일"이라는 통설은 쓰지 말 것** —
2024·2025년처럼 둘째 목요일인 해가 있다.

그래서 해마다 손댈 것이 없다. 해가 바뀌면 저절로 다음 회차를 가리킨다.

**연기됐을 때만 손으로 적는다.** 지금까지의 예외는 전부 연기였다 — 2005 APEC,
2010 G20, 2017 포항 지진(하루 전 긴급 연기), 2020 코로나(2주). `index.html`의
`const SUNEUNG_MOVED`에 실제 시행일을 적으면 그것이 이기고, 그 날이 지나면 저절로
무시되므로 지우는 것을 잊어도 된다.

매일 도는 갱신이 `tests/test_dday.py --date`로 셈한 날을 한국어 위키백과의
학년도별 인포박스와 대조한다. 어긋나면 실패한다 — 연기됐거나 평가원이 관례를
벗어나 공고했다는 뜻이므로 `SUNEUNG_MOVED`를 채울 때다. **위키백과 값을 화면에
쓰지는 않는다.** 누구나 고칠 수 있는 문서라 그것을 그대로 믿을 수는 없고, 공식
쪽에는 기계가 읽을 출처가 아예 없다(평가원 RSS 없음, 공공데이터포털에도 없음).

## 이 실행 환경에서 막히는 것 (해보기 전에 알아 둘 것)

**`api.github.com`은 세션에서 쓸 수 없다.** 프록시가 가로채 403을 돌려주고,
직접 넣은 토큰은 무시된다(토큰 있을 때와 없을 때 응답이 같다). 읽기는
`mcp__github__*` 도구로, 쓰기는 워크플로의 `GITHUB_TOKEN`으로 한다.

**git은 ref를 만들고 옮길 수만 있다.** 브랜치 push·force push는 되지만
**태그 갱신과 ref 삭제는 통과되지 않는다** — `send-pack: unexpected disconnect`로
끊긴다. 태그를 옮기거나 브랜치를 지워야 하면 일회성 워크플로를 만들어
`GITHUB_TOKEN`으로 API를 부르고, 쓴 뒤에 그 워크플로를 지운다.

## 판본 올리기

`android/app/build.gradle`의 `versionCode`·`versionName`을 올려 main에 밀면
`android.yml`이 전부 한다 — 서명 빌드 → 지문 확인 → 릴리스 생성 → APK 첨부 →
`app/latest.json` 커밋. **릴리스가 먼저, 명세 커밋이 나중이다.** 뒤집으면 명세가
아직 없는 주소를 가리키는 동안 앱이 그것을 읽고 내려받기에 실패한다.

사용자 알림 막대에 뜰 한 줄은 커밋 본문의 `Notes:` 줄에서 가져온다. 커밋 제목은
영어 관례라 그대로 쓰면 화면에 "Release 4.9"가 나간다.

## 고친 뒤에는

화면(`index.html`·`sw.js`·`s/`)을 건드렸으면 `python3 tests/run.py`를 돌린다
(28종, 3분 남짓). 무엇을 지키는 시험인지는 `tests/README.md`에 있다.
인라인 스크립트를 고쳤으면 `python3 tools/check_csp.py --fix`로 CSP 해시를
다시 맞춘다 — 어긋난 채로 나가면 그 스크립트가 통째로 막힌다.
자료·과목 페이지를 다시 만들었으면 `tools/build_fonts.py`가 **맨 나중**이다.

자세한 것은 `docs/build.md`(파일 구성·자료 갱신·검색), `docs/android.md`(앱과
배포), `tests/README.md`(시험)에 있다.
